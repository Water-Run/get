# get v4.0.0 开发目标（历史记录）

> 2026-09-10：本文的问题与真实会话已纳入 [v3.2.0 开发与清理记录](DEVELOPMENT-v3.2.0.md)。当前版本方向以该记录为准；本文保留原始诊断，不再作为未来 v4 的约束。

整理日期：2026-09-07（Asia/Shanghai）。依据当日真实会话、当前 3.1.0 源码与本机 Linux 复现，不是发布说明，也不是实现计划。

**v4.0.0 的主目标只有一条：任务稍微复杂一点，get 仍然要能用。**

v3 已经把类型化 Harness、强制只读策略和原生写拒绝做成了产品骨架。v3.1.0 继续收紧策略绕过。实验室里的短问答、本仓库上的「目录下的代码组成」都能过。真实工作区一旦变成多语言、带嵌套 `build` / `.venv` 的仓库，同一类问题会在默认预算内耗尽轮次，退出 126，不给出答案。

v4 不把「再收紧一层安全」或「再加一种渲染」当作主线。主线是：在不关闭强制只读的前提下，让真实本地检查走完「取证 → 解释 → 回答」。

## 产品承诺与实际差距

README 的定位是「从这台计算机上取任何东西」，示例包含「当前目录的代码结构」。v3.0.1 验证把「代码组成」写成已覆盖意图，代表命令是有界 `find` / `rg` 加只读 `sed` / AWK。[提示词](src/harness_prompt.nim)甚至直接教模型：

```text
Composition: find -printf | sed | sort | uniq -c;
skip .git/.ci/.release/build/dist/node_modules/__pycache__.
```

[运行时](src/harness_runtime.nim)对含「组成」的请求会强制再解释，不允许把原始列表当最终答案。默认预算却是 3 轮模型、8 次工具、30 秒命令、1 MiB 捕获。

本仓库的 CLI 场景 `natural_code_composition_zh`（问句「目录下的代码组成」）只在 get 自己的小树上断言回答里出现 `nim`。它证明了玩具树能过，不能证明真实项目能过。

## 触发会话

配置为当时本机 `get config` 的有效值，不是实验室覆盖：

| 项 | 值 |
|---|---|
| 二进制 | 已安装 get 3.1.0 |
| 模型 | `deepseek-v4-flash` @ `https://api.deepseek.com` |
| harness | `auto` |
| shell | `fish` |
| max-rounds / max-tool-calls / max-parallel | `3` / `8` / `4` |
| command-timeout / max-output-bytes | `30` / `1048576` |
| manual-confirm / double-check | `false` / `false` |

同一会话里，简单问题可用：

```text
~/Project> get 目录下有几个什么项目
→ ls -la /home/waterrun/Project
→ 列出目录，正常结束
```

同一会话里，问题只复杂了一点就失败：

```text
~/Project/yy-subsystems (main)> get "项目的语言组成"
```

`yy-subsystems` 是多系统单仓：Rust、Python、C#、Node、Java、CMake/C++ 并存，且含嵌套 `build/`、`.venv/`、`node_modules/`、第三方 `_deps`。这不是边界刁难，是 get 自称要服务的日常项目。

失败过程按 Harness 轮次还原：

1. **第 1 轮（两条都过策略，开始执行）**  
   模型几乎照抄提示词里的组成配方：`find … -prune … -printf '%f\n' | sed … | sort | uniq -c | sort -rn | head`，并行再列顶层目录。策略允许这条管道。Linux 上 Harness 执行带 `bwrap --ro-bind / /`。小输入的 `sort` 能在内存里完成；一旦 `find` 扫到大体量树，GNU sort 要写临时文件。本机用同一沙箱复现：

   ```text
   sort: 无法在 '/home/waterrun/.local/tmp' 创建临时文件: 只读文件系统
   ```

   管道最后一级仍可能以 0 退出并交出空统计。模型看不到「组成」，只能再试。

2. **第 2 轮（1 条拒绝，1 条执行）**  
   模型把 `sort` 改成 `env TMPDIR=/tmp sort`。策略判定：`executable 'env' is not in the read-only allowlist`。即便放行，`/tmp` 同样被只读绑定，本机复现仍是「无法在 '/tmp' 创建临时文件」。并行的 `head README.md` / `find systems` 跑了，但没有组成数字。

3. **第 3 轮（1 条拒绝，1 条执行，预算耗尽）**  
   模型改用内存聚合：`awk -F. 'NF>1 {c[$NF]++} … END {for (e in c) print …}'`。策略判定：`awk program is outside the pure selector subset`。并行的清单搜索跑了，却把 `YouYouLeaseCore/build/release/_deps/**/CMakeLists.txt`、`.venv/**/Makefile` 一并倒出。运行时在最后一轮工具之后不再给模型作答，把原始观察加上 `[harness model-turn budget exhausted]` 当作失败输出。最后一批含策略拒绝，进程退出 **126**。用户没有得到语言组成答案。

对照同一条命令在当前策略下的判定（fish，本机 `checkReadOnlyCommand`）：

| 模型实际发出的命令 | 策略 | 沙箱 / 观察 |
|---|---|---|
| 提示词配方 `find \| sed \| sort \| uniq -c` | 允许 | 大输入时 sort 无法写临时文件 |
| `env TMPDIR=/tmp sort` | 拒绝：`env` 不在白名单 | 即使执行，`/tmp` 仍只读 |
| `awk` 关联数组 + `END` 计数 | 拒绝：超出纯选择子集 | 这是不写盘的聚合路径，被关掉 |
| `tokei .` | 允许 | 提示词未教；不能假定主机有该程序 |
| 清单 `find` 匹配 `Cargo.toml` / `package.json` / `CMakeLists.txt` | 允许 | 未剪掉嵌套 `build` / `.venv` / `_deps`，输出被产物淹没 |

这是闭锁，不是偶发模型胡写：

- 提示词指定的聚合路径需要可写临时目录。
- 策略禁止给 `sort` 设 `TMPDIR` / `-T`，也禁止 `env`。
- 策略禁止 awk 做内存计数。
- 原生沙箱把整个根文件系统（含 `$TMPDIR` 与 `/tmp`）绑成只读。
- 默认 3 轮在两次拒绝重试后耗尽，没有作答轮。

## 根因

按对源码的对应关系写，不按表面日志写。

### 1. 提示词、策略、沙箱三者不一致

[harness_prompt.nim](src/harness_prompt.nim) 把 `find -printf | sed | sort | uniq -c` 写成组成问题的标准动作，并要求跳过若干顶层目录名。[command_policy.nim](src/command_policy.nim) 允许这条管道，但：

- `sort -T` / `--temporary-directory` 被显式拒绝（策略测试里就有这两条）。
- `env` 不在只读白名单。
- awk 只允许无副作用的字段选择和 `print`，不允许 `{c[$NF]++}` 与 `END` 循环。
- [exec.nim](src/exec.nim) 在 Linux 上用 `bwrap --ro-bind / /`，不提供私有 tmpfs，也不改写子进程 `TMPDIR`。

教出去的命令在真实体量下跑不通；模型改用的两条补救命令又被策略挡住。

### 2. 默认预算只够「一次命中」，不够「受阻后仍回答」

默认 `max-rounds=3`、`max-tool-calls=8`。[harness_runtime.nim](src/harness_runtime.nim) 在 `turn >= maxTurns` 时直接结束：把最后一批观察格式化输出，附加预算耗尽标记，退出码在全 0 时改成 1，否则沿用批次里的非 0（策略拒绝是 126）。

被拒绝的调用仍计入工具次数；含「组成」的请求会强制 `continue`，却不预留最后一轮纯文本作答。结果是：已经拿到部分证据（顶层目录、部分清单），用户仍然只看到命令噪声和 126。

### 3. 观察质量按玩具树设计，真实树会淹没模型

提示词的 prune 列表是 `./.git`、`./.ci`、`./.release`、`./build`、`./dist`、`./node_modules`、`./__pycache__`。它匹配顶层名字，不匹配 `systems/apps/YouYouLeaseCore/build/...`、`.venv`、`archive`、CMake `_deps`。

回灌模型的观察有 12 KiB 压缩上限；预算耗尽时展示给用户的是未压缩的原始批次。清单搜索因此在终端里刷出第三方 `CMakeLists.txt`，对「语言组成」几乎没有信息量。

### 4. 验收没有覆盖「略复杂」的真实项目

`extension_composition` 和 `natural_code_composition_zh` 都在小的确定性目录或 get 源码树上通过。v3 的 47 项真实模型 Harness 同样不包含带嵌套构建产物的多语言单仓。安全侧有数千条成对允许/拒绝；可用性侧没有对等的真实树门槛。v3.1 收紧绕过之后，这条可用性裂缝被放大，而不是被测到。

## v4.0.0 成功标准

发布 v4.0.0 之前，下列条件必须同时成立。数字可以在实现时微调，方向不能改成「把预算调大就算完成」。

1. **回放门槛。** 在 `yy-subsystems` 这类真实多语言仓库中，默认配置下 `get "项目的语言组成"`（及同义中英文问法）以退出码 0 结束，给出可核对的语言/扩展组成，而不是预算耗尽或 126。回答必须能从观察解释，而不是模型空想。
2. **简单问题不回退。** `get 目录下有几个什么项目`、设备 IP、Git 分支与未提交文件、有界性能快照等 v3 日常场景仍一次工具调用内完成。
3. **教出去的命令必须能跑。** 提示词推荐的组成/统计管道，在 Linux 原生只读沙箱里对超过内存阈值的输入也能得到非空统计；模型为补救 sort 而写的 `env TMPDIR=…` 不应成为唯一活路。
4. **有证据就要作答。** 只要已经执行过至少一次成功观察，Harness 不得在无最终文本答案的情况下以 `htBudgetExhausted` 结束。预算将尽时必须把已有观察交给模型作答，或由运行时给出基于观察的降级说明。
5. **产物不得冒充项目。** 组成类答案不得被嵌套 `build`、`.venv`、`node_modules`、`__pycache__`、CMake `_deps`、打包目录主导。清单搜索若保留，必须先按仓库卫生裁剪。
6. **强制只读不降级。** 不提供关闭语义策略的开关；未知可执行文件、写入、执行、安装、信号、命令替换仍失败关闭。每一条新允许的聚合能力必须带成对拒绝用例。Linux/macOS 原生写拒绝保留；Windows 无对等 OS 沙箱仍是已知边界，不是 v4 用「放宽策略」换可用性的理由。
7. **验收树升级。** 保留本仓库小树回归，并增加至少一棵含嵌套构建产物的多语言夹具（或受控的 `yy-subsystems` 回放）。真实模型门禁必须包含本次触发问句，而不是只跑 get 自己的树。

## v4 工作面

实现顺序与具体补丁在后续计划里拆。v4.0.0 的开发范围围着上面的成功标准转，而不是另开产品线。

### A. 只读沙箱里的有界聚合

让「统计 / 排序 / 去重」在根文件系统只读时仍可运行。方向包括：为观测进程提供私有 tmpfs 并固定 `TMPDIR`；或允许明确无 I/O 的 awk 计数程序；或两者都做、用测试锁死边界。禁止把通用 `env`、任意 `-T` 路径、脚本解释器当作解决方案。

### B. 提示词与策略对齐

提示词只准推荐当前策略和沙箱下可执行的命令。组成问题应优先走「窄清单 + 有界计数」，而不是先全树 `find` 再在失败后连退两次。`tokei` 已在白名单，可以写成交主机可选加速，不能写成硬依赖。

### C. 真实树观察卫生

默认跳过嵌套构建与依赖目录，而不是只 prune 顶层名字。观察回灌应保留对回答有用的前缀和计数，而不是把 1 MiB 产物路径交给用户当失败报告。

### D. Harness 收尾语义

区分「工具预算」和「作答预算」。策略拒绝要反馈给模型，但不能把最后一轮作答挤掉。`auto` 在组成/汇总/比较类意图上，成功观察之后必须解释；失败时输出的是答案或明确的能力边界，不是命令日志。

### E. 以真实项目为门禁

把本次会话固化成回归：策略探针（允许的组成管道、拒绝的 `env` 与越权 awk）、沙箱中的大输入 `sort`、夹具树上的语言组成、以及 DeepSeek 真实回放。实验室小树继续留作快测，不能再单独代表「代码组成已可用」。

## 非目标

v4.0.0 明确不做下面这些事：

- 变成可写代码、改配置、装软件的 agent。
- 关闭或可配置地绕过强制只读策略。
- 放开通用 awk/脚本/命令替换来「提高灵活性」。
- 只把 `max-rounds` / `max-tool-calls` 调大当作修复。
- 以 Windows 原生文件系统沙箱对等、或代码签名，作为本版本主目标。
- 绑定单一模型供应商；DeepSeek 是本次复现所用配置，不是协议。

v3 的安全模型、缓存身份、发布装配流程沿用。能不动的模块不要为了这条主目标顺手重构。

## 与既有版本的关系

| 版本 | 已经完成的事 | 留给 v4 的缺口 |
|---|---|---|
| v3.0.x | 统一 Harness、强制语义只读、预算与并行、发布骨架 | 可用性按短命令和本仓库验收 |
| v3.1.0 | Markdown 渲染；堵住可复现的策略绕过与执行漏洞 | 收紧后，提示词配方与沙箱更加不一致 |
| v4.0.0 | 以本次会话为定义，修复「复杂一点就没法用」 | 主版本号反映的是产品能否用于真实项目，不是又一层装饰 |

当前仓库若仍有 v3.1 文档回填、持久化或代理边角问题，它们可以在 v3 维护线或 v4 旁路修复，但不得写成 v4.0.0 的主目标。

## 判定句

v4.0.0 可以发布，当且仅当：用默认配置，在真实的略复杂项目上问「这是什么、由什么组成、现在什么状态」，get 能给出基于本地观察的答案，并且强制只读仍然关闭失败。做不到这一点，就还不是 v4。

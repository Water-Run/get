# get v4.0.0 开发记录（进行中）

范围：[V4-DEVELOPMENT-PLAN.md](V4-DEVELOPMENT-PLAN.md)。只做查询，减少误拦截；不是允许修改宿主或远端的通用执行代理。当前开发分支 `develop/v4.0.0`，基线为 `2e403a7ab09ff2e3bed52fd0c0b8385bae8667d7`。尚未发布、安装或宣称通过最终门槛。

## 已实现的工作路径

- CLI 查询业务移到 `query_service.nim`；新提案、审核改写和缓存计划通过统一授权入口。
- 五个类型化查询工具共享 schema 和解析器：环境、文件分页、文件搜索、直接 argv、Shell。旧工具名只作兼容别名。
- 普通环境名称不再限制为四个变量。环境观察区分缺失、空值和脱敏值；进程输出也做环境秘密值脱敏。
- 原生文件工具支持分页、中文/空格路径、嵌套忽略目录、字面搜索及输出上限。文件操作在可取消的内部工作进程中执行，FIFO 不阻塞读取。
- Linux 受限计算通过 bwrap、用户/PID/网络等命名空间、只读宿主文件系统、seccomp 和资源限制实现；只有完整工作进程探测通过才启用。私有临时区由父进程管理和清理。macOS/Windows 暂不开放任意脚本。
- 普通 Git status/diff 在私有元数据副本中读取宿主文件，禁用仓库/global 配置中的可执行助手，不修改真实索引。结果明确标记为禁用可执行 filter 与子模块检查的视图；这不是承诺复现自定义过滤器的执行结果。
- 默认 6 轮取证、16 次实际工具执行、4 路并行，另有作答轮；拒绝、复用和实际执行分别计数。失败最多有 2 次局部恢复机会。
- 新增 `query-timeout`（默认 120 秒），模型请求和排队工具使用剩余期限，并为回答预留时间。
- stdout/stderr 分离；模型反馈保留头尾，区别于实际捕获截断。`required` 和 `evidence_key` 跟踪必要事实是否修复。
- 缓存 schema 4 保存类型化查询计划，重放仍重新授权；身份包含工具、后端能力及执行配置。显式结果缓存显示采集时间。
- `diagnostics` 可输出同一事件流与预算汇总；默认终端只显示步骤和答案。

## 当前验证证据

原始记录保存在 `.ci/v4/`，这些是开发快照结果，不是最终发布报告。

- v3.2 策略基线：40 个正常提案中 12 个被原分类器拒绝。这是提案计数，不是自然语言任务失败率。
- 本地受限计算首轮 5 项通过：Python/命令替换、私有临时区、宿主文件及符号链接写保护、网络/Unix socket、PID/资源/超时/输出限制。
- 本地类型化工具 12 项通过；新增运行时 4 项通过，覆盖预算分账、必要证据修复、局部截断恢复和头尾反馈。
- fish 最新 7 项定向 CLI 回归通过：环境、原始变量展开、文件分页、搜索、受限计算、未知工具恢复、重复观察复用。
- Git 本地 2 项通过：status/工作区 diff/暂存区 diff 与真实结果核对；恶意 clean filter、diff/textconv、fsmonitor 未执行，真实索引字节未变。
- `source-p4b` 快照的 Linux/macOS 全部原生测试与 CLI 通过；Windows 全套只留下 CRLF 和 Git 快速同尺寸修改两个问题，均已修复。
- `source-p4c` 在 Linux 与 Windows 复验 Git、文件、配置、行为及完整 CLI 均通过。Windows 使用同一用户已有交互登录上下文测试 DPAPI；测试计划任务已删除，未修改桌面会话。
- 新增整次查询期限回归：模型延迟 5 秒、查询期限 2 秒时，在约 2 秒返回失败，不输出迟到答案。
- Qwen `qwen3.8-27b` 经已有 DGX Spark SGLang 服务试跑，环境、代码组成/行数、Git diff 和宿主进程场景已通过；旧版出现环境误拒绝和 Git 超时。试跑不是完整门槛，全部原始记录保留。
- DeepSeek 试跑遇到 HTTP 402；用户已告知余额恢复，将用于最终二进制正式回放。
- Linux 测试机没有 `/proc/pressure/memory`；远程验证脚本明确记录该能力缺失，仍检查可用内存并串行低优先级编译。共享桌面的检查要求不变。

## 尚未完成的门槛

- Git 及原生工作进程的新路径完成跨平台及取消/清理复验；诊断事件计数、普通退出状态解释继续核对。
- 扩展边界回归、旧 CLI 拒绝预期迁移、整次查询期限及缓存重验证回归。
- 至少 40 个自然语言场景、每个 3 次的真实提供商对照，独立真值、完整失败分母、耗时/请求/恢复/token/内存指标。
- 最终三平台 CI、真实提供商回放和二进制哈希绑定。版本/README/man/装配已改为 v4，提供商记录保持 pending，旧版验证哈希已清空。
- 用户的活动安装程序、配置、密钥和桌面状态均保持原样。

## 真实模型驱动的修正

Qwen 第一轮 6 场景试跑：v3.2 与 v4 均完成 5/6；发生策略拒绝的任务分别为 4/6 与 0/6。v4 的失败是模型最终回答正确，但必要文件读取的 ENOENT 被归为执行失败。现在文件读取明确返回 `exists=false` / `not_found` / `osNoMatch`，只根据真实 OS 的不存在错误判断；目录与权限错误仍失败。该场景的原始失败记录保留，待真实模型复验。新增原生和 CLI 回归已通过。

另修正 stderr 反馈压缩的 UTF-8 边界。第一次 CI（35557669024）在验证过程中因上述源码修正主动取消；不计为通过。下一轮使用修正后的提交。

CI 35557896366 的 macOS 全门槛通过；Linux Bash 通过，但旧版 fish 因启动时尝试创建只读配置/历史目录，导致 14 个断言被警告文本污染。修正为 `--private` 加私有 XDG 启动存储，并在查询前恢复原始 XDG 值；HOME 不变。新增回归覆盖空配置目录、含空格/引号的值、原始环境保持和无宿主目录写入，本地执行测试通过。下一次 CI 继续验证旧 fish。

CI 35558448177 的 Linux（Bash/fish）、macOS、Windows 全部通过。随后的真实 DeepSeek 对照发现同一 `evidence_key` 已成功后，被失败的补充检查覆盖的问题；改为保留该事实的成功证据，仍要求其他必要事实各自有证据。运行时和 CLI 回归通过。失败观察也直接提示修复时保持原来的事实标识，避免模型给替代查询另起标识。旧回放与其失败分母保留，最终验证必须重新绑定修正后的产物。

未显式命名的必要事实使用原调用 ID，并在观察中返回该标识供替代查询修复。工具说明明确宿主诊断与 Git status/diff 优先使用独立进程调用，避免复合 Shell 进入隔离环境后误用视图或反复尝试仓库可执行 filter。

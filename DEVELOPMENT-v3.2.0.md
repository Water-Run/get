# get v3.2.0 开发与清理记录

2026-09-10。承接 [原 v4 问题记录](V4-DEVELOPMENT-GOAL.md)，版本号按本次开发要求改为 v3.2.0。目标是让“取某段代码、解释项目、查看系统某种情况”正常完成，并清理旧的运行逻辑和检验负担。

## 已确认的问题与对应修改

| 问题 | 依据 | v3.2.0 的处理 |
|---|---|---|
| 多语言项目的语言组成失败 | 9 月 7 日会话：sort 临时文件写入失败，env 与 AWK 补救又被拒绝 | POSIX 检查获得私有磁盘临时目录；Linux 沙箱只为该目录开放写入，结束后删除；AWK 支持内存聚合 |
| 普通统计被当成执行危险代码 | 当前策略只接受字段选择，拒绝计数数组、END、tolower、算术 | 用独立的 AWK 能力校验替换狭小的选择器配方；允许变量、关联数组、纯函数与聚合，拦截文件写入、外部程序和输入列表改写 |
| 环境查询误拒绝 | 9 月 10 日“deepseek版本”调用 env/set 被拒绝 | env 和无赋值的 POSIX set 查询可执行；模型也能直接看到配置中的模型标识，避免为此遍历环境 |
| Git 文件名查询要求多余参数 | 历史会话中的 diff-files --name-only 被拒绝 | 元数据查询不再强求 diff 渲染禁用参数；git show REV:PATH 可直接读取版本中的代码；读取工作树并可能触发 clean filter 的路径仍有独立限制 |
| 系统设置读取与变更混为一谈 | 当前工具列表缺少 GNOME 设置读取 | 支持 gsettings 查询、dconf read/list/dump、gnome-extensions list/info/version；不会为开发重载或修改桌面扩展 |
| 有证据却没有答案 | 最后一轮执行工具后直接返回预算耗尽和命令噪声 | auto/loop/parallel 在观察后作答；独立收尾轮禁用工具；收尾失败时给出有界的已有证据及非零状态 |
| 是否解释由中英文关键词列表决定 | runtime 匹配“组成”“情况”“summary”等字样 | 删除问句关键词路由；direct 显式保留一次调用及原始输出语义 |
| 根据模型名称判断强弱、改变采样 | utils 中品牌/版本/后缀规则；配置着色；弱模型提示；Qwen 名称采样分支 | 全部删除。模型标识原样传递，非空即中性显示；不另建 Flash 白名单 |
| 旧 Markdown 代码块与 HTML 标记触发命令 | v2 extractAgentAction、extractOutputMode 及兼容解码 | 删除旧执行协议；所有代码示例和旧标记均为回答文本；旧 legacy 配置值迁移为 json |
| 检验集重复、绑定旧行为 | get_test.py 大型选项矩阵；模型“强/弱”清单；提示词逐句断言；CI 固定 DeepSeek 47 项 | 用小型真实任务回放替换重复矩阵，保留协议/执行/持久化等确定性回归；模型名称透明传输；CI 读取本版本实测记录而非特定品牌/旧计数 |

本地 get.log 的观察范围为 2026-08-25 至 2026-09-10，共 455 条记录，含 332 条 `--version` 记录。日志按命令记录，混有开发验证，不能作为自然用户查询成功率；旧的批次连带拒绝、部分硬件/服务读取问题已在之前版本修过，本次不重复计作新修复。公开文档不附原始私人日志。

## 验收方式

- 实际执行只读沙箱中的小输入强制落盘排序，核实宿主文件写入仍被阻止、临时目录已清理。
- 计数、算术、纯函数、环境读取采用允许/拒绝配对测试，并检查真实执行结果。
- 默认模式检查“取证后回答”；一个工具/一轮取证仍能完成禁用工具的最终回答，direct 的原始输出边界仍可核验。
- 模拟服务核对任意模型标识原样发送、请求没有按名称添加 temperature，普通代码示例不执行。
- 真实服务使用带嵌套 build、target、.venv、node_modules、_deps 的多语言夹具，校验确切扩展名数量、代码条件、环境值、无匹配结果、系统内存及预算收尾；另回放真实多语言仓库。
- 本地只做聚焦验证，单次编译使用 nice -n 10 与 --parallelBuild:1，检查 MemAvailable/内存压力；完整 Linux/Windows/macOS 套件在 CI 运行。

发布证据见 [验证记录](VALIDATION-v3.2.0.md) 和 [发布记录](RELEASE-v3.2.0.md)。历史发布报告作为归档保留，不作为新版本测试结果，也不将旧基准数据装入本次发布包。
